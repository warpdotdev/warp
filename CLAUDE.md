# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The canonical engineering guide for this repo lives in **[AGENTS.md](./AGENTS.md)** — the single
source of truth for build/run/test commands, architecture (GUI + TUI front-ends over a shared
`warp_core`/`warpui` core), coding style, terminal-model locking, feature flags, and the PR workflow.
Its full contents are imported below so they apply to every Claude Code session; keep edits in
`AGENTS.md`, not here, so the two never drift.

@AGENTS.md
