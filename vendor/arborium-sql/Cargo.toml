# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "arborium-sql"
version = "2.13.0"
authors = ["Amos Wenger <amos@bearcove.eu>"]
build = "build.rs"
include = [
    "src/**/*",
    "grammar/**/*",
    "queries/**/*",
    "corpus/**/*",
    "sample.*",
    "samples/**/*",
    "build.rs",
    "Cargo.toml",
    "README.md",
    ".arborium-hash",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "SQL grammar for arborium (tree-sitter bindings)"
homepage = "https://github.com/bearcove/arborium"
documentation = "https://docs.rs/arborium-sql"
readme = "README.md"
keywords = [
    "tree-sitter",
    "sql",
    "syntax-highlighting",
    "query",
]
categories = [
    "parsing",
    "text-processing",
]
license = "MIT"
repository = "https://github.com/bearcove/arborium"

[package.metadata.cargo-shear]
ignored = ["arborium-sysroot"]

[lib]
name = "arborium_sql"
path = "src/lib.rs"

[dependencies.tree-sitter-language]
version = "0.1"

[dev-dependencies.arborium-test-harness]
version = "2.13.0"

[dev-dependencies.libtest-mimic]
version = "0.8"

[build-dependencies.cc]
version = "1"

[target.'cfg(target_family = "wasm")'.dependencies.arborium-sysroot]
version = "2.13.0"
